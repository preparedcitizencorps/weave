# WEAVE — zero-lib mesh middleware in pure C
No ESP-IDF. No malloc. No Arduino. No external code whatsoever.
Target: ≤32 KB binary, ≤4 KB RAM, runs on $10 ESP32-S3.
Foundational layer for open-source civilian ISR suite (Prepared Citizen Corps).
